# 🤖 بوت تليجرام لفحص البطاقات عبر Braintree

هذا المشروع هو بوت تليجرام متكامل لفحص صلاحية بطاقات الائتمان باستخدام بوابة الدفع **Braintree**، مع واجهة سهلة الاستخدام، ودعم للفحص الفردي والجماعي، وعرض إحصائيات مباشرة.

## ✨ المميزات الرئيسية

- **بوابة Braintree مخصصة**: يستخدم البوت بوابة دفع حقيقية من موقع `petcostumecenter.com` لإجراء عمليات فحص دقيقة.

- **فحص فردي وجماعي**: يدعم فحص بطاقة واحدة عبر أمر `/chk` أو فحص قائمة بطاقات من ملف نصي.

- **إحصائيات مباشرة**: يعرض تحديثات حية لعملية الفحص الجماعي كل ثانية، بما في ذلك التقدم والوقت المتبقي.

- **تنسيق احترافي**: يتم عرض النتائج بتنسيق واضح ومنظم، مع فصل البطاقات المقبولة في ملف خاص.

- **سهولة الاستخدام**: واجهة بسيطة ومباشرة مع أوامر واضحة.

- **جاهز للنشر**: تم إعداد المشروع ليكون جاهزاً للنشر بسهولة على منصة **Render**.

## 📂 محتويات المشروع

| الملف/المجلد | الوصف |
| --- | --- |
| `bot.py` | الملف الرئيسي لتشغيل بوت التليجرام. يحتوي على جميع الأوامر والمعالجات. |
| `braintree_checker.py` | وحدة الفحص الأساسية التي تتفاعل مع بوابة Braintree لإجراء عمليات الفحص. |
| `requirements.txt` | قائمة بجميع المكتبات اللازمة لتشغيل المشروع. |
| `render.yaml` | ملف التكوين الخاص بمنصة Render لنشر البوت بسهولة. |
| `.env.example` | ملف نموذجي لمتغيرات البيئة المطلوبة. |
| `README.md` | ملف التوثيق هذا. |
| `data/` | مجلد لحفظ ملفات الجلسات والنتائج. |
| `logs/` | مجلد لحفظ سجلات تشغيل البوت. |

## 🚀 كيفية التشغيل والنشر على Render

اتبع هذه الخطوات لنشر البوت وتشغيله بشكل دائم على الإنترنت باستخدام منصة Render.

### الخطوة 1: تجهيز المشروع

1. **إنشاء حساب على GitHub**: إذا لم يكن لديك حساب، قم بإنشاء حساب جديد على [GitHub](https://github.com).

1. **إنشاء مستودع جديد (New Repository)**: قم بإنشاء مستودع جديد على GitHub بالاسم الذي تفضله (مثلاً: `braintree-bot`).

### الخطوة 2: رفع ملفات المشروع إلى GitHub

1. **تنزيل ملفات المشروع**: قم بتنزيل جميع ملفات المشروع التي تم إنشاؤها.

1. **رفع الملفات**: ارفع جميع الملفات والمجلدات إلى المستودع الذي أنشأته على GitHub.

### الخطوة 3: إعداد متغيرات البيئة

1. **الحصول على توكن بوت التليجرام**: تحدث مع [BotFather](https://t.me/BotFather) على تليجرام لإنشاء بوت جديد والحصول على التوكن الخاص به.

1. **إعداد متغيرات البيئة على Render**:
  - اذهب إلى حسابك على [Render](https://render.com).
  - أنشئ خدمة جديدة من نوع **Web Service**.
  - اربط الخدمة بمستودع GitHub الذي أنشأته.
  - في قسم **Environment**، أضف المتغيرات التالية:

| المفتاح (Key) | القيمة (Value) |
| --- | --- |
| `` | `التوكن_الخاص_بك_من_BotFather` |
| `ADMIN_USER_ID` | `معرف_حسابك_على_تليجرام` |

**ملاحظة**: لا تحتاج إلى إضافة متغيرات Braintree لأنها مدمجة في الكود.

### الخطوة 4: النشر والتشغيل

1. **تأكيد الإعدادات**: تأكد من أن Render يستخدم الإعدادات التالية (يجب أن يتم اكتشافها تلقائياً من ملف `render.yaml`):
  - **Build Command**: `pip install -r requirements.txt`
  - **Start Command**: `python bot.py`

1. **بدء النشر**: اضغط على **Create Web Service**.

سيقوم Render تلقائياً بتثبيت المكتبات وتشغيل البوت. يمكنك متابعة سجلات التشغيل (Logs) للتأكد من أن كل شيء يعمل بشكل صحيح.

## 📋 الأوامر وطريقة الاستخدام

- `/start`: لعرض رسالة الترحيب والتعليمات.

- `/chk <بيانات_البطاقة>`: لفحص بطاقة واحدة. الصيغة: `رقم_البطاقة|MM|YY|CVV`.

- `/stats`: لعرض إحصائيات الفحص الخاصة بك.

- **إرسال ملف**: أرسل ملف `.txt` يحتوي على قائمة من البطاقات (كل بطاقة في سطر) لبدء الفحص الجماعي.

## 🔧 المطور

تم تطوير هذا البوت بواسطة **علاوي الاسطوره** ([@B11HB](https://t.me/B11HB)).

